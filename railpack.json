{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["pip install -r pjmapp/requirements.txt"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
    }
  },
  "deploy": {
    "startCommand": "flask --app py/app.py run"
  }
}
